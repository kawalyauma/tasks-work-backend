{
  "name": "tasks-work-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": ["web"],
  "scripts": {
    "dev": "wrangler dev",
    "dev:api": "wrangler dev",
    "dev:web": "npm run dev --workspace web",
    "build:web": "npm run build --workspace web",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "db:local": "wrangler d1 migrations apply tasks-work-db --local",
    "db:remote": "wrangler d1 migrations apply tasks-work-db --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260903.1",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4",
    "wrangler": "^4.128.0"
  }
}
